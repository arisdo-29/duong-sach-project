# Mẫu cấu hình – copy thành .env rồi điền mật khẩu thật. KHÔNG commit file .env.
# Hướng dẫn chi tiết: docs/05-database-va-deploy.md mục 4.
# Mật khẩu có ký tự đặc biệt ; = { } [ ] : / \ thì bọc trong ngoặc nhọn: password={Mat;Khau}

DATABASE_URL="sqlserver://localhost:1433;database=DuongSach;user=sa;password=<matkhau>;encrypt=true;trustServerCertificate=true"

# Domain công khai dùng để sinh URL mã QR di sản
PUBLIC_BASE_URL="http://localhost:5173"
