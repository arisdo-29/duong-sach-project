name: "🐞 Báo lỗi"
description: Lỗi tìm thấy khi test
title: "[BUG] "
labels: ["type: bug", "status: todo"]
body:
  - type: input
    id: where
    attributes:
      label: Ở đâu
      placeholder: "POST /api/admin/booths trên Vercel Preview / màn hình Quản lý di sản"
    validations:
      required: true
  - type: textarea
    id: steps
    attributes:
      label: Các bước tái hiện
      value: |
        1. 
        2. 
    validations:
      required: true
  - type: textarea
    id: expected
    attributes:
      label: Kết quả mong đợi vs thực tế (dán response / log / ảnh)
    validations:
      required: true
