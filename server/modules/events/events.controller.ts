/**
 * KHUNG SẴN – chủ module: C · FR-04 CRUD sự kiện (issue #11)
 *
 * Chỉ đọc req và trả res (vai trò @RestController). Không chứa nghiệp vụ,
 * không gọi Prisma. Express 5 tự chuyển lỗi async sang error-handler nên
 * không cần try/catch.
 */

// TODO(#11): export async function list/getById/create/update/remove
export {};
