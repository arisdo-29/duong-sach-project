/**
 * KHUNG SẴN – chủ module: C · FR-01 CRUD gian hàng (issue #8)
 *
 * Chỉ đọc req và trả res (vai trò @RestController). Không chứa nghiệp vụ,
 * không gọi Prisma. Express 5 tự chuyển lỗi async sang error-handler nên
 * không cần try/catch.
 */

// TODO(#8): export async function list/getById/create/update/remove
export {};
