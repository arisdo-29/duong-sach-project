import type { OpenApiPaths } from '../../core/openapi.js';

/**
 * KHUNG SẴN – chủ module: D · FR-03 CRUD thiết bị (issue #10)
 *
 * Mô tả endpoint cho /api-docs.html. Thêm endpoint mới thì bổ sung vào đây,
 * tài liệu tự cập nhật (không phải sửa core/openapi.ts).
 */
export const assetsPaths: OpenApiPaths = {
  // TODO(#10): mô tả các endpoint của /api/admin/assets
};

export default assetsPaths;
