/**
 * KHUNG SẴN – chủ module: D · FR-03 CRUD thiết bị (issue #10)
 *
 * Chỉ đọc req và trả res (vai trò @RestController). Không chứa nghiệp vụ,
 * không gọi Prisma. Express 5 tự chuyển lỗi async sang error-handler nên
 * không cần try/catch.
 */

// TODO(#10): export async function list/getById/create/update/remove
export {};
