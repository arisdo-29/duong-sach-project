import type { OpenApiPaths } from '../../core/openapi.js';

/**
 * KHUNG SẴN – chủ module: D · FR-05/FR-06 hồ sơ đề xuất (issue #12)
 *
 * Mô tả endpoint cho /api-docs.html. Thêm endpoint mới thì bổ sung vào đây,
 * tài liệu tự cập nhật (không phải sửa core/openapi.ts).
 */
export const proposalsPaths: OpenApiPaths = {
  // TODO(#12): mô tả các endpoint của /api/admin/proposals
};

export default proposalsPaths;
