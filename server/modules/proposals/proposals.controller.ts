/**
 * KHUNG SẴN – chủ module: D · FR-05/FR-06 hồ sơ đề xuất (issue #12)
 *
 * Chỉ đọc req và trả res (vai trò @RestController). Không chứa nghiệp vụ,
 * không gọi Prisma. Express 5 tự chuyển lỗi async sang error-handler nên
 * không cần try/catch.
 */

// TODO(#12): export async function list/getById/create/update/remove
export {};
