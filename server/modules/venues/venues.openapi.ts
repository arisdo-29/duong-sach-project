import type { OpenApiPaths } from '../../core/openapi.js';

/**
 * KHUNG SẴN – chủ module: C · FR-02 CRUD địa điểm tổ chức (issue #9)
 *
 * Mô tả endpoint cho /api-docs.html. Thêm endpoint mới thì bổ sung vào đây,
 * tài liệu tự cập nhật (không phải sửa core/openapi.ts).
 */
export const venuesPaths: OpenApiPaths = {
  // TODO(#9): mô tả các endpoint của /api/admin/venues
};

export default venuesPaths;
