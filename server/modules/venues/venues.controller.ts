/**
 * KHUNG SẴN – chủ module: C · FR-02 CRUD địa điểm tổ chức (issue #9)
 *
 * Chỉ đọc req và trả res (vai trò @RestController). Không chứa nghiệp vụ,
 * không gọi Prisma. Express 5 tự chuyển lỗi async sang error-handler nên
 * không cần try/catch.
 */

// TODO(#9): export async function list/getById/create/update/remove
export {};
