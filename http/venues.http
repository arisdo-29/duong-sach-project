### FR-02 CRUD địa điểm tổ chức – /api/admin/venues (issue #9)
# Chủ module: C. Mở bằng REST Client (VS Code) hoặc HTTP Client (IntelliJ).
# Đổi @host sang domain production khi test bản deploy.

@host = http://localhost:5173

# TODO(#9): thêm request mẫu cho từng endpoint, kèm cả ca lỗi 400 / 404 / 409
