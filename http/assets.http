### FR-03 CRUD thiết bị – /api/admin/assets (issue #10)
# Chủ module: D. Mở bằng REST Client (VS Code) hoặc HTTP Client (IntelliJ).
# Đổi @host sang domain production khi test bản deploy.

@host = http://localhost:5173

# TODO(#10): thêm request mẫu cho từng endpoint, kèm cả ca lỗi 400 / 404 / 409
